[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pingerplot"
version = "1.3.0"
description = "A continuous-traceroute network path monitor (MTR-style) with a Tkinter GUI: per-hop latency, loss, jitter and MOS over time."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "James Cupps" }]
keywords = ["traceroute", "ping", "mtr", "network", "latency", "packet-loss", "monitor", "icmp", "mos", "dscp", "windows"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Win32 (MS Windows)",
    "Environment :: X11 Applications",
    "Intended Audience :: System Administrators",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: MIT License",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS :: MacOS X",
    "Programming Language :: Python :: 3",
    "Topic :: System :: Networking :: Monitoring",
]
dependencies = []          # standard library only

[project.optional-dependencies]
dev = ["pytest"]

[project.gui-scripts]
pingerplot = "pingerplot.gui:main"

[project.scripts]
pingerplot-headless = "pingerplot.headless:main"

[project.urls]
Homepage = "https://github.com/jamesccupps/PingerPlot"
Source = "https://github.com/jamesccupps/PingerPlot"
Issues = "https://github.com/jamesccupps/PingerPlot/issues"

[tool.setuptools.packages.find]
include = ["pingerplot*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--import-mode=importlib"
